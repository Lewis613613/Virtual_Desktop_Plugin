<Window x:Class="VirtualDesktopPanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VirtualDesktopPanel"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="CanResize"
        Background="Transparent"
        Deactivated="OnDeactivated"
        KeyDown="OnKeyDown">

    <Window.Resources>
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30FFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="PanelBorder"
            CornerRadius="12"
            BorderThickness="1"
            BorderBrush="#30FFFFFF">
        <Border.Background>
            <SolidColorBrush x:Name="PanelBackgroundBrush" Color="#1a1a2e"/>
        </Border.Background>

        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar (drag handle) -->
            <Border Grid.Row="0"
                    Background="Transparent"
                    MouseLeftButtonDown="OnTitleBarMouseDown"
                    CornerRadius="12,12,0,0">
                <Grid Margin="12,0">
                    <TextBlock Text="Virtual Desktop Panel"
                               Foreground="#888"
                               FontSize="12"
                               VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <Button Content="S" Style="{StaticResource TitleBarButton}"
                                Click="OnSettingsClick"
                                ToolTip="Settings"/>
                        <Button Content="X" Style="{StaticResource TitleBarButton}"
                                Click="OnCloseClick"
                                ToolTip="Close"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Scrollable icon area -->
            <ScrollViewer Grid.Row="1"
                          x:Name="IconScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          Background="Transparent">
                <local:IconGridPanel x:Name="IconGrid"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
